import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("compress","PNG"); export const metadata:Metadata={title:"Compress PNG Images Online",description:guide.intro,alternates:{canonical:"/compress-png"}}; export default function Page(){return <ToolGuidePage guide={guide} tool={<ImageTool initialMode="compress" compressOnly fixedFormat="image/png" acceptedTypes="image/png" uploadHint="PNG images supported" previewResult/>}/>;}
