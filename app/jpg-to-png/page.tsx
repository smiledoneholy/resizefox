import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("convert","JPG","PNG"); export const metadata:Metadata={title:"Convert JPG to PNG Online",description:guide.intro,alternates:{canonical:"/jpg-to-png"}}; export default function Page(){return <ToolGuidePage guide={guide} tool={<ImageTool initialMode="convert" convertOnly fixedFormat="image/png" acceptedTypes="image/jpeg" uploadHint="Choose a JPG; the result will be PNG" previewResult/>}/>;}
