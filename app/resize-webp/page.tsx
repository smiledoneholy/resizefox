import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("resize","WebP"); export const metadata:Metadata={title:"Resize WebP Images Online",description:guide.intro,alternates:{canonical:"/resize-webp"}}; export default function Page(){return <ToolGuidePage guide={guide} tool={<ImageTool initialMode="resize" resizeOnly fixedFormat="image/webp" acceptedTypes="image/webp" uploadHint="WebP images supported" previewResult/>}/>;}
