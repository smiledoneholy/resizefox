import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("compress","WebP"); export const metadata:Metadata={title:"Compress WebP Images Online",description:guide.intro,alternates:{canonical:"/compress-webp"}}; export default function Page(){return <ToolGuidePage imageReference guide={guide} tool={<ImageTool initialMode="compress" compressOnly fixedFormat="image/webp" acceptedTypes="image/webp" uploadHint="WebP images supported" previewResult/>}/>;}
