import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("resize","PNG"); export const metadata:Metadata={title:"Resize PNG Images Online",description:guide.intro,alternates:{canonical:"/resize-png"}}; export default function Page(){return <ToolGuidePage imageReference guide={guide} tool={<ImageTool initialMode="resize" resizeOnly fixedFormat="image/png" acceptedTypes="image/png" uploadHint="PNG images supported, including transparency" previewResult/>}/>;}
