import type { Metadata } from "next"; import ImageTool from "../ImageTool"; import ToolGuidePage from "../tools/ToolGuidePage"; import { formatGuide } from "../tools/guide-data";
const guide=formatGuide("convert","WebP","JPG"); export const metadata:Metadata={title:"Convert WebP to JPG Online",description:guide.intro,alternates:{canonical:"/webp-to-jpg"}}; export default function Page(){return <ToolGuidePage imageReference guide={guide} tool={<ImageTool initialMode="convert" convertOnly fixedFormat="image/jpeg" acceptedTypes="image/webp" uploadHint="Choose a WebP; the result will be JPG" previewResult/>}/>;}
